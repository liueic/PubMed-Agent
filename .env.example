# ============================================
# PubMed Agent 环境变量配置示例
# Environment Variables Configuration Example
# ============================================

# ============================================
# 必需配置 (Required Configuration)
# ============================================

# OpenAI API 密钥（必需）
OPENAI_API_KEY=your_openai_api_key_here

# PubMed Email 地址（强烈推荐，必需以符合NCBI使用政策）
# NCBI要求提供真实的email地址
PUBMED_EMAIL=your_email@example.com

# ============================================
# 可选但推荐的配置 (Optional but Recommended)
# ============================================

# PubMed API 密钥（可选，但推荐以提升速率限制：从3次/秒提升至10次/秒）
# 获取方式：登录NCBI账户 -> 我的NCBI -> API密钥
PUBMED_API_KEY=your_ncbi_api_key_here

# ============================================
# OpenAI 相关配置 (OpenAI Related Configuration)
# ============================================

# OpenAI 模型名称（默认：gpt-4o）
OPENAI_MODEL=gpt-4o

# 自定义OpenAI API Endpoint（可选）
# OPENAI_API_BASE=

# ============================================
# PubMed 相关配置 (PubMed Related Configuration)
# ============================================

# PubMed 工具名称（默认：pubmed_agent）
PUBMED_TOOL_NAME=pubmed_agent

# ============================================
# 向量数据库配置 (Vector Database Configuration)
# ============================================

VECTOR_DB_TYPE=chroma
CHROMA_PERSIST_DIRECTORY=./data/chroma
FAISS_INDEX_PATH=./data/faiss.index

# ============================================
# 嵌入模型配置 (Embedding Model Configuration)
# ============================================

EMBEDDING_MODEL=text-embedding-v4
EMBEDDING_DIMENSION=1536
# DASHSCOPE_API_KEY=your_dashscope_api_key_here
# EMBEDDING_API_BASE=

# ============================================
# Agent 行为配置 (Agent Behavior Configuration)
# ============================================

MAX_RETRIEVE_RESULTS=10
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
TEMPERATURE=0.0
LOG_LEVEL=INFO
