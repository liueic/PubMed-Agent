# ReAct PubMed Agent - Environment Variables Configuration
# 复制此文件为 .env 并填入你的API密钥
# Copy this file to .env and fill in your API keys

# ============================================
# OpenAI Configuration (必需 / Required)
# ============================================
# 获取方式: https://platform.openai.com/api-keys
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI模型选择 (可选，默认为gpt-4o)
# OpenAI model selection (optional, default: gpt-4o)
# 可选值: gpt-4o, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o

# ============================================
# PubMed Configuration (可选 / Optional)
# ============================================
# PubMed API要求提供邮箱（建议设置）
# PubMed API requires an email (recommended to set)
PUBMED_EMAIL=your_email@example.com

# PubMed工具名称 (可选，默认: pubmed_agent)
# PubMed tool name (optional, default: pubmed_agent)
PUBMED_TOOL_NAME=pubmed_agent

# PubMed API密钥 (可选，通常不需要)
# PubMed API key (optional, usually not needed)
# PUBMED_API_KEY=your_pubmed_api_key_here

# ============================================
# Vector Database Configuration (可选 / Optional)
# ============================================
# 向量数据库类型: chroma 或 faiss
# Vector database type: chroma or faiss
VECTOR_DB_TYPE=chroma

# ChromaDB持久化目录 (可选，默认: ./data/chroma)
# ChromaDB persist directory (optional, default: ./data/chroma)
CHROMA_PERSIST_DIRECTORY=./data/chroma

# FAISS索引路径 (可选，默认: ./data/faiss.index)
# FAISS index path (optional, default: ./data/faiss.index)
FAISS_INDEX_PATH=./data/faiss.index

# ============================================
# Embedding Configuration (可选 / Optional)
# ============================================
# 嵌入模型 (可选，默认: text-embedding-3-small)
# Embedding model (optional, default: text-embedding-3-small)
# 可选值: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
EMBEDDING_MODEL=text-embedding-3-small

# 嵌入维度 (可选，默认: 1536)
# Embedding dimension (optional, default: 1536)
EMBEDDING_DIMENSION=1536

# ============================================
# Text Processing Configuration (可选 / Optional)
# ============================================
# 文本分块大小 (可选，默认: 1000)
# Text chunk size (optional, default: 1000)
CHUNK_SIZE=1000

# 文本分块重叠大小 (可选，默认: 200)
# Text chunk overlap (optional, default: 200)
CHUNK_OVERLAP=200

# 最大检索结果数 (可选，默认: 10)
# Maximum retrieve results (optional, default: 10)
MAX_RETRIEVE_RESULTS=10

# ============================================
# LLM Configuration (可选 / Optional)
# ============================================
# 温度参数 (可选，默认: 0.0，用于减少幻觉)
# Temperature (optional, default: 0.0, for reducing hallucinations)
# 范围: 0.0 - 2.0，0.0表示最确定性的回答
TEMPERATURE=0.0

# ============================================
# Logging Configuration (可选 / Optional)
# ============================================
# 日志级别 (可选，默认: INFO)
# Log level (optional, default: INFO)
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================
# Custom API Endpoint (可选 / Optional)
# ============================================
# 自定义API端点，支持兼容OpenAI API的服务
# Custom API endpoint, supports OpenAI-compatible APIs
# 示例 / Examples:
# - 本地部署模型: http://localhost:8000/v1
#   Local model: http://localhost:8000/v1
# - Azure OpenAI: https://YOUR_RESOURCE.openai.azure.com/
# - 其他兼容服务: https://api.example.com/v1
#    Other compatible services: https://api.example.com/v1
# 留空则使用默认OpenAI API (https://api.openai.com/v1)
# Leave empty to use default OpenAI API (https://api.openai.com/v1)
OPENAI_API_BASE=
